use std::{os::unix::process::CommandExt, process::Command};

use super::*;

#[test]
fn call_cargo() {
    // let mut cmd = Command::new("cargo");
    // let rc = cmd.exec();
    // assert(cmd.execute_check_exit_status_code(0).is_err());
    // command("cargo");
}
